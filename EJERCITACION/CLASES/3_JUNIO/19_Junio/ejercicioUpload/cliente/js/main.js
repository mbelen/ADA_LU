console.log("me cargué");
/*
var servidor = "http://localhost:3000/"

$(document).ready(function() {
    //se hace el pedido al backend de todos los generos para cargalos en el listado de géneros
    $.getJSON(servidor,
        function(data) {
        	console.log(data.imagenes)
        	for(i=0;i<data.imagenes.length;i++){
            	var img = `<figure><img src=${data.imagenes[i]}></figure>`
            	console.log(img)
            	$('#guardoImagen').append(img);
            }
    });
});
*/


